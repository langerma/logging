output {
	# Emit events to stdout for easy debugging of what is going through
	# logstash.
	file {
		path => "/tmp/logstash.log"
	}
	stdout { codec => rubydebug }

	# This will use elasticsearch to store your logs.
	elasticsearch { hosts => [ "10.0.0.220:9200" ] }
}
