filter {
    if [type] == "freenas" {
        grok {
            match => ["message", "%{SYSLOG5424PRI}%{SYSLOGLINE}"]
            add_tag => ["freenas"]
        }
        multiline {
            pattern => "^\s+|^\t\s+"
            what => "previous"
        }
    }
}
