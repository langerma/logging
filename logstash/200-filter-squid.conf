filter {
	if [type] == "squid" {
        	grok {
                	patterns_dir => ["/usr/local/etc/logstash/patterns"]
                        match => ["message", "%{SQUIDLOG}"]
                        add_tag => ["squid"]
                }
		date {
	                match => [ "timestamp", "UNIX" ]
		}
	}
}
